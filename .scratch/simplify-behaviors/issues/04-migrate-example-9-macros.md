# 04 — Migrate example 9 :macros to declarative map schema

**What to build:** `examples/9.edn` `:macros` region is migrated to the map-of-behaviors schema. The output keymap is regenerated from `examples/9_in.keymap` and the integration test for example 9 passes.

**Blocked by:** 03 — Migrate example 1 :macros to declarative map schema and delete render-macro

**Status:** ready-for-agent

- [ ] `examples/9.edn` `:macros` converted to map-of-behaviors schema
- [ ] `examples/9_out.keymap` regenerated; integration seam passes
